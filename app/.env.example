# Flask Secret Key - 生成方式: python3 -c "import secrets; print(secrets.token_hex(32))"
# 生产环境必须设置为一个复杂且唯一的随机字符串
SECRET_KEY=

# 调试模式: true / false (生产环境必须为 false)
DEBUG=false

# 应用配置
HOST=0.0.0.0
PORT=5000
